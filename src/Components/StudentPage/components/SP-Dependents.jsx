import React from "react";

export default function SPDependents() {
   let dependents = null;
   if (dependents) {
      return <div>{dependents}</div>;
   } else {
      return <div>None</div>;
   }
}
